# ============================================================
# Loan Approval Prediction & RAG Advisory System
# Honours Year Project | University of Johannesburg | 222086329
# ============================================================

# ── Core Data & ML ──────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
scipy==1.13.0
imbalanced-learn==0.12.3          # SMOTE / class-imbalance handling

# ── Gradient Boosting ───────────────────────────────────────
xgboost==2.0.3
lightgbm==4.3.0

# ── Explainability (SHAP) ───────────────────────────────────
shap==0.45.0
matplotlib==3.9.0
seaborn==0.13.2

# ── Fairness Analysis ───────────────────────────────────────
fairlearn==0.10.0                 # equalized odds, demographic parity

# ── HuggingFace / LLM (Pipeline 2) ─────────────────────────
transformers==4.41.2              # Mistral-7B-Instruct inference
accelerate==0.30.1                # multi-GPU / device mapping
bitsandbytes==0.43.1              # 4-bit / 8-bit quantization (local inference)
sentencepiece==0.2.0              # tokenizer dependency
huggingface-hub==0.23.2

# ── Embeddings & Vector Retrieval (Experiment 2 baseline) ───
sentence-transformers==3.0.1      # all-MiniLM-L6-v2 embeddings
chromadb==0.5.0                   # vector-search baseline for retrieval eval

# ── Database / ORM ──────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1                   # DB migrations
psycopg2-binary==2.9.9            # PostgreSQL driver (swap for sqlite if needed)

# ── PDF Processing ──────────────────────────────────────────
pypdf==4.2.0                      # text extraction from regulatory PDFs
pdfplumber==0.11.1                # table + layout-aware PDF parsing
pymupdf==1.24.3                   # fitz: fast page rendering / OCR prep
reportlab==4.2.0                  # PDF report generation

# ── DOCX / Word ─────────────────────────────────────────────
python-docx==1.1.2                # read/write .docx advisory reports

# ── API / Web Layer (optional Gradio UI) ────────────────────
gradio==4.36.1                    # interactive demo UI (mirrors Outamation work)
fastapi==0.111.0                  # REST API wrapper if needed
uvicorn==0.30.1

# ── Experiment Tracking & Evaluation ────────────────────────
mlflow==2.13.1                    # experiment logging (classifiers + retrieval)
optuna==3.6.1                     # hyperparameter search alternative to GridSearchCV

# ── NLP / Text Utilities (TF-IDF pipeline) ──────────────────
nltk==3.8.1                       # tokenisation, stopwords for custom TF-IDF
regex==2024.5.15

# ── Inter-Rater Reliability (Experiment 3) ──────────────────
krippendorff==0.6.1               # Cohen's Kappa / alpha for evaluator agreement
pingouin==0.5.4                   # statistical tests

# ── Jupyter / Notebooks ─────────────────────────────────────
jupyter==1.0.0
ipykernel==6.29.4
ipywidgets==8.1.3

# ── Dev / Quality ───────────────────────────────────────────
python-dotenv==1.0.1
tqdm==4.66.4
loguru==0.7.2
pytest==8.2.2
black==24.4.2